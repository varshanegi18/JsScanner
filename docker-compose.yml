version: '3.9'

services:
  scanner:
    build: .
    image: domain-scanner:latest
    container_name: domain-scanner
    ports:
      - "8000:8000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M